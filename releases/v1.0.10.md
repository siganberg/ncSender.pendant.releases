The only commit since v1.0.9 is "Skip release when no changes since last tag" which modified `.scripts/release.sh` — a purely internal CI/build change with no user-facing impact.

There are no user-facing changes to generate release notes for. Based on your rule #4 (exclude internal/chore commits unless they impact users), this release would have an empty changelog.

You may want to skip the v1.0.10 release, or include the staged change in `MachineState.h` (shown in git status) if that's intended for this release. Want me to check what that uncommitted change contains?
